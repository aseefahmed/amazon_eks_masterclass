apiVersion: apps/v1
kind: Deployment
metadata:
  name: "deployment-two"
  namespace: "sample-app"
spec:
  selector:
    matchLabels:
      app: "sample-app-two"
  replicas: 5
  template:
    metadata:
      labels:
        app: "sample-app-two"
    spec:
      containers:
      - image: aseefahmed/ecommerce:app_1
        imagePullPolicy: Always
        name: "sample-contaainer"
        ports:
        - containerPort: 8080
          protocol: TCP


--- 

apiVersion: v1
kind: Service
metadata:
  name: "service-two"
  namespace: "sample-app"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "sample-app-two"